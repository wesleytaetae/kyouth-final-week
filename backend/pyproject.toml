[project]
name = "simple-fastapi-backend"
version = "0.1.0"
description = "Minimal FastAPI backend with SQLite"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.116.0",
    "uvicorn[standard]>=0.35.0",
]

[tool.uv]
package = false
dev-dependencies = []
